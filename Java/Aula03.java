//Operadores
public class Aula03{
      public static void main(String[] args) {
        //Operadores de atribuição: 
        int = numero 10; 
        //Aditivo 
        numero = numero + 10; // ou numero += 10
        //Subtrativo 
        numero = numero - 10; // ou numero -= 10
        //multiplicativo
        numero = numero * 10; // ou numero *= 10 
        // diviso 
        numero =  numero / 10; // ou numero /= 10
        //modular
        numero = numero % 2; // ou numero %= 10, divide o número por dois e pega o resto da divisão

        //Operador aritmetico: 
        //soma
        int soma = 10 + 20; 
        //subtração 
        int subtracao = 20 - 10; 
        //multiplicação
        int multiplicacao = 20 * 10; 
        //divisão
        int divisao = 20 / 10; 
        //modular 
        int modulo = 20 % 10; 
        //operador unario
        int unario = 10;
        unario++; //atribui + 1 ao valor de unario 10 + 1 |  POSTFIX
        ++unario; // mesmo resultado | PREFIX
        -unario // negativando o valor     

        //operadores logicos
        /*
        AND 
        V and V = V 
        V and F = F 
        F anf V = F
        F and F = F
        
        OR
        V or V = V
        v oe F = V
        F or V = v
        F or F = F

        XOR => calcula as diferenças
        V xor V= F
        V xor F = V
        F xor V = V
        F xor F = F

        Negação ! 
        !V = F 
        !F = V 

        // && é o AND 
        Ex: System.out.println(true && true); 
        retorna = tru
         
        // || é o OR
         Ex: System.out.println(true || false);
         retorna = false 

        // != é o xor 
         Ex: System.out.println(true != false);
         retorna = true 

        // ! é a negação 
          Ex: System.out.println(!true);
          retorna = false 

        // Operadores relacionais 

        // == é o de igual 
        Ex: System.out.println(10 == 10);
        retorna: true; 

        // != é o de diferença 
         Ex: System.out.println(10 != 10);
        retorna: false; 

        // > é o a maior 
         Ex: System.out.println(10 > 20);
        retorna: false; 

        // < é o menor 
         Ex: System.out.println(10 < 20);
        retorna: true;
        
        //<= menor igual 
        Ex: System.out.println(10 <= 15);
        retorna: true;

        //>=
        Ex: System.out.println(15 >= 10);
        retorna: true;

        //Operadores ternarios 
        int idade = 10; 
        System.out.println(idade >= 18 ? "Maior de idade" : "Menor de idade");

        */ 
    
    }
}